# `@kugutu/compiler`

Minimal headless compiler for turning Kugutu character documents into charbundles, composed SVG assets, and single-file charpacks.

Current API:

- `buildCharacterBundle(document)`
- `composeCharacterSvg(document, svgText)`
- `buildCharacterPack(document, svgText)`
