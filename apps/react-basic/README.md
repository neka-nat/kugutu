# `apps/react-basic`

Minimal React demo for `@kugutu/react`.

The demo loads `avatar.charpack` and mounts it through `<KugutuCharacterPack />`.

Run:

```bash
pnpm run dev:react-basic
```

Then open the Vite URL printed in the terminal, usually `http://localhost:4174/`.
